import 'package:flutter_test/flutter_test.dart';
import 'package:test/app.dart';

void main() {
  test('app widget can be constructed', () {
    expect(const ObdSimulatorApp(), isA<ObdSimulatorApp>());
  });
}
